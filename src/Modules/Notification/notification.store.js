/* eslint no-shadow: ["error", { "allow": ["state"] }] */

// import * as types from './mutation-types'

const state = {}
const mutations = {}
const actions = {}

const getters = {}

export default {
  state,
  mutations,
  actions,
  getters
}
